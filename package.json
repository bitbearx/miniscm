{
  "name": "miniscm",
  "displayName": "%extension.displayName%",
  "description": "%extension.description%",
  "version": "0.1.2",
  "publisher": "bitbearx",
  "license": "MIT",
  "files": [
    "out/src",
    "package.nls.json",
    "package.nls.zh-cn.json",
    "LICENSE.md",
    "README.md"
  ],
  "engines": {
    "vscode": "^1.85.0"
  },
  "extensionKind": [
    "workspace"
  ],
  "categories": [
    "Source Control"
  ],
  "activationEvents": [
    "onStartupFinished",
    "onCommand:miniscm.showFileHistory",
    "onCommand:miniscm.compareFileWithRef",
    "onCommand:miniscm.openCommitDetails"
  ],
  "main": "./out/src/extension.js",
  "contributes": {
    "commands": [
      {
        "command": "miniscm.showFileHistory",
        "title": "%command.showFileHistory%",
        "category": "%command.category%",
        "icon": "$(history)"
      },
      {
        "command": "miniscm.compareFileWithRef",
        "title": "%command.compareFileWithRef%",
        "category": "%command.category%",
        "icon": "$(git-compare)"
      }
    ],
    "menus": {
      "explorer/context": [
        {
          "command": "miniscm.showFileHistory",
          "when": "resourceScheme == file || explorerResourceIsFolder",
          "group": "z_miniscm@20"
        },
        {
          "command": "miniscm.compareFileWithRef",
          "when": "resourceScheme == file && !explorerResourceIsFolder",
          "group": "z_miniscm@21"
        }
      ],
      "editor/title/context": [
        {
          "command": "miniscm.showFileHistory",
          "when": "resourceScheme == file",
          "group": "z_miniscm@20"
        },
        {
          "command": "miniscm.compareFileWithRef",
          "when": "resourceScheme == file",
          "group": "z_miniscm@21"
        }
      ]
    }
  },
  "scripts": {
    "compile": "tsc -p ./",
    "prepack": "npm run compile",
    "watch": "tsc -watch -p ./",
    "test": "npm run compile && node --test out/tests/*.test.js",
    "vscode:prepublish": "npm run compile"
  },
  "devDependencies": {
    "@types/node": "^22.13.0",
    "@types/vscode": "1.85.0",
    "typescript": "^5.7.3"
  }
}